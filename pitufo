-- PITUFO WX7: VERSIÓN ULTRA-MOLESTA (FORCE)
-- Si no funcionaba, este método "bruto" lo hará funcionar.

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ContentProvider = game:GetService("ContentProvider")

local player = Players.LocalPlayer
local pgui = player:WaitForChild("PlayerGui")

-- 1. FORZAR SONIDO (ID de Error muy agudo)
local sound = Instance.new("Sound")
sound.SoundId = "rbxassetid://4612375231" 
sound.Volume = 10 -- Volumen al máximo
sound.Name = "AlertaPitufo"
sound.Parent = player.Character or workspace

-- Precargar sonido para que no falle al sonar
ContentProvider:PreloadAsync({sound})

-- 2. CREAR MENSAJE GIGANTE EN PANTALLA
local screenGui = Instance.new("ScreenGui", pgui)
screenGui.Name = "PitufoAlert"

local alertLabel = Instance.new("TextLabel", screenGui)
alertLabel.Size = UDim2.new(1, 0, 0.2, 0)
alertLabel.Position = UDim2.new(0, 0, 0.4, 0)
alertLabel.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
alertLabel.TextColor3 = Color3.new(1, 1, 1)
alertLabel.TextScaled = true
alertLabel.Font = Enum.Font.GothamBold
alertLabel.Text = "NOS QUERIAS ESTAFAR? PITUFO?"
alertLabel.Visible = false

-- 3. BUCLE DE SPAM CADA 1 SEGUNDO
task.spawn(function()
    while true do
        -- Sonido
        sound:Play()
        
        -- Flash visual (Parpadeo)
        alertLabel.Visible = true
        print("🚨 NOS QUERIAS ESTAFAR? PITUFO? 🚨")
        warn("⚠️ NOS QUERIAS ESTAFAR? PITUFO? ⚠️")
        
        task.wait(0.5)
        alertLabel.Visible = false -- Efecto de parpadeo
        
        task.wait(0.5) -- Completa el segundo
    end
end)
