-- PITUFO WX7: OMNIPOTENT RGB OVERLOAD (FINAL VERSION)
-- ADVERTENCIA: CRASH INMINENTE / SATURACIÓN TOTAL

repeat task.wait() until game:IsLoaded()

local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local parent = (gethui and gethui()) or game:GetService("CoreGui")

-- 1. SOBRECARGA DE HILOS (CPU KILLER)
-- Esto genera millones de cálculos por frame para "congelar" el procesador
RunService.Stepped:Connect(function()
    for i = 1, 1000000 do 
        local _ = math.sin(i) * math.acos(math.random())
    end
end)

-- 2. GENERADOR MASIVO DE OBJETOS (RAM & GPU CRASHER)
-- Crea miles de interfaces cada segundo para saturar la memoria
task.spawn(function()
    local gui = Instance.new("ScreenGui", parent)
    gui.Name = "PITUFO_FINAL_STRIKE"
    gui.DisplayOrder = 1000000
    
    while true do
        for i = 1, 500 do
            local f = Instance.new("Frame", gui)
            f.Size = UDim2.new(math.random(), 0, math.random(), 0)
            f.Position = UDim2.new(math.random(), 0, math.random(), 0)
            f.BackgroundColor3 = Color3.fromHSV(tick() % 1, 1, 1) -- RGB NIVEL DIOS
            f.BorderSizePixel = 0
            
            local t = Instance.new("TextLabel", f)
            t.Size = UDim2.new(1, 0, 1, 0)
            t.Text = "ESTAFA DETECTADA"
            t.TextScaled = true
            t.BackgroundTransparency = 1
            t.TextColor3 = Color3.new(1,1,1)
            
            -- Eliminar rápido para que el procesador trabaje el doble creando y borrando
            task.delay(0.05, function() f:Destroy() end)
        end
        task.wait() -- Velocidad máxima
    end
end)

-- 3. DISTORSIÓN DE ILUMINACIÓN EXTREMA
task.spawn(function()
    while true do
        Lighting.Brightness = 9999
        Lighting.ClockTime = math.random(0, 24)
        Lighting.ExposureCompensation = math.random(-10, 10)
        Lighting.Ambient = Color3.fromHSV(tick() % 1, 1, 1)
        task.wait()
    end
end)

-- 4. CACOFONÍA DE AUDIO (10 SONIDOS SIMULTÁNEOS)
for i = 1, 10 do
    local s = Instance.new("Sound", workspace)
    s.SoundId = "rbxassetid://4612375231"
    s.Volume = 10
    s.PlaybackSpeed = 0.2 + (i * 0.2)
    s.Looped = true
    s:Play()
end

-- 5. MENSAJE FINAL (HACKER OVERLAY)
local overlay = Instance.new("ScreenGui", parent)
local label = Instance.new("TextLabel", overlay)
label.Size = UDim2.new(1, 0, 1, 0)
label.BackgroundColor3 = Color3.new(0, 0, 0)
label.BackgroundTransparency = 0.2
label.Text = "SYSTEM FAILURE\nBY PITUFO WX7\n\nNOS QUERIAS ESTAFAR?"
label.TextColor3 = Color3.new(1, 0, 0)
label.TextScaled = true
label.Font = Enum.Font.Code
label.ZIndex = 9999999

-- Parpadeo de Texto
RunService.RenderStepped:Connect(function()
    label.TextColor3 = Color3.fromHSV(tick() % 1, 1, 1)
    label.Rotation = math.sin(tick() * 10) * 5
end)

print("PITUFO WX7: EJECUCIÓN DIVINA INICIADA. ADIÓS AL SISTEMA.")
