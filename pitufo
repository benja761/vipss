-- PITUFO WX7: MODO APOCALIPSIS (FREEZE & HACKER STYLE)
-- ADVERTENCIA: Este script está diseñado para saturar el cliente de Roblox.

repeat task.wait() until game:IsLoaded()

local parent = (gethui and gethui()) or game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local HttpService = game:GetService("HttpService")

-- Limpieza de versiones anteriores
local old = parent:FindFirstChild("PITUFO_TERMINAL_OVERRIDE")
if old then old:Destroy() end

local gui = Instance.new("ScreenGui")
gui.Name = "PITUFO_TERMINAL_OVERRIDE"
gui.IgnoreGuiInset = true
gui.DisplayOrder = 9999999
gui.Parent = parent

-- FONDO DE "MATRIX" SANGRIENTO
local bg = Instance.new("Frame")
bg.Size = UDim2.new(1, 0, 1, 0)
bg.BackgroundColor3 = Color3.fromRGB(10, 0, 0)
bg.BorderSizePixel = 0
bg.Parent = gui

-- 1. PROCESO DE CONGELAMIENTO (Saturación de Memoria/CPU)
-- Crea miles de cálculos inútiles para laguear el motor
task.spawn(function()
    while true do
        for i = 1, 500000 do
            local _ = math.sin(i) * math.tan(i) -- Operaciones basura
        end
        task.wait(0.001)
    end
end)

-- 2. SPAM DE INSTANCIAS (Agotamiento de Memoria)
task.spawn(function()
    while task.wait(0.01) do
        local p = Instance.new("Part")
        p.Name = "PITUFO_VIRUS"
        p.Transparency = 1
        p.Anchored = true
        p.CanCollide = false
        p.Parent = workspace
        
        -- Generar alertas en consola sin parar
        for i = 1, 50 do
            warn("!!! CRITICAL ERROR: PITUFO_WX7_EXPLOIT_DETECTED !!!")
            error("SYSTEM OVERHEATING - OVERRIDING FAN CONTROL")
        end
    end
end)

-- 3. INTERFAZ "HACKER" AGRESIVA
local mainText = Instance.new("TextLabel")
mainText.Size = UDim2.new(1, 0, 1, 0)
mainText.BackgroundTransparency = 1
mainText.Text = "SYSTEM HIJACKED\nBY PITUFO WX7\n\nNOS QUERIAS ESTAFAR?"
mainText.Font = Enum.Font.Code
mainText.TextColor3 = Color3.fromRGB(255, 0, 0)
mainText.TextScaled = true
mainText.ZIndex = 100
mainText.Parent = gui

local stroke = Instance.new("UIStroke", mainText)
stroke.Thickness = 5
stroke.Color = Color3.new(0,0,0)

-- 4. EFECTOS VISUALES DE "VIRUS"
RunService.RenderStepped:Connect(function()
    local t = tick()
    -- Flash de pantalla (Estroboscópico)
    bg.BackgroundColor3 = (math.sin(t * 30) > 0) and Color3.new(0,0,0) or Color3.new(0.5, 0, 0)
    
    -- Movimiento errático del texto
    mainText.Position = UDim2.new(math.random(-2, 2)/100, 0, math.random(-2, 2)/100, 0)
    mainText.Rotation = math.sin(t * 50) * 5
    
    -- Distorsión de Iluminación (Glitch)
    Lighting.Brightness = math.random(0, 10)
    Lighting.ExposureCompensation = math.random(-5, 5)
end)

-- 5. SONIDO DE ALARMA DESTRUCTORA
local s = Instance.new("Sound", workspace)
s.SoundId = "rbxassetid://4612375231"
s.Volume = 10
s.PlaybackSpeed = 0.5 -- Sonido lento y aterrador
s.Looped = true
s:Play()

-- 6. BLOQUEO DE CHAT Y CONTROLES (Simulado)
task.spawn(function()
    local lp = game.Players.LocalPlayer
    if lp then
        pcall(function()
            lp.Character.Humanoid.WalkSpeed = 0
            lp.Character.Humanoid.JumpPower = 0
        end)
    end
end)

print("PITUFO WX7: TOTAL OVERRIDE LOADED.")
